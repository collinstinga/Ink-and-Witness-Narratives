rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Firestore is a server-only backend for this application. Browser and
    // mobile SDK access is denied; the restricted Firebase Admin identity is
    // authorized separately through Google Cloud IAM.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
